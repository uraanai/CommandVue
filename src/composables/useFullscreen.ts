/**
 * Re-export of `@vueuse/core`'s `useFullscreen` so callers can import from a
 * stable `@/composables/useFullscreen` path. Replace with a custom wrapper
 * here if/when the API needs project-specific defaults.
 */
export { useFullscreen } from "@vueuse/core";
