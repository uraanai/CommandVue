/**
 * Tool module barrel. Tool implementations land here in subsequent commits:
 *   - Phase 7b: measure-distance, draw-polygon
 */
export type { Tool, ToolContext, ToolId, ToolSetupResult } from "./types";
