version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    # Dependency updates flow through `develop` like all other work, then ride the
    # normal release branch into `main` (see CLAUDE.md → Branch and workflow). This
    # keeps `main` from diverging on manifests, so release PRs don't conflict on
    # package.json / pnpm-lock.yaml. NOTE: Dependabot reads THIS file only from the
    # default branch (`main`), so this setting takes effect once it lands on `main`.
    target-branch: "develop"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "06:00"
      timezone: "UTC"
    open-pull-requests-limit: 10
    commit-message:
      prefix: "chore"
      include: "scope"
    labels:
      - "dependencies"
    groups:
      # Major bumps for `vue` and `eslint` always land as individual PRs
      # (each one needs deliberate migration work). Minors and patches still
      # group together to keep the noise down.
      vue:
        patterns:
          - "vue"
          - "vue-*"
          - "@vue/*"
          - "@vitejs/plugin-vue"
        update-types:
          - "minor"
          - "patch"
      vite:
        patterns:
          - "vite"
          - "vite-plugin-*"
          - "@vitejs/*"
      cesium:
        patterns:
          - "cesium"
          - "vite-plugin-static-copy"
      maplibre:
        patterns:
          - "maplibre-*"
      turf:
        patterns:
          - "@turf/*"
      tanstack:
        patterns:
          - "@tanstack/*"
      eslint:
        patterns:
          - "eslint"
          - "eslint-*"
          - "@vue/eslint-config-*"
        update-types:
          - "minor"
          - "patch"
      commitlint:
        patterns:
          - "@commitlint/*"
      types:
        patterns:
          - "@types/*"

  - package-ecosystem: "github-actions"
    directory: "/"
    # Same rationale as the npm block above — target `develop`, not the default branch.
    target-branch: "develop"
    schedule:
      interval: "weekly"
      day: "monday"
    commit-message:
      prefix: "ci"
    labels:
      - "dependencies"
      - "github-actions"
